# pgpeach
An experiment in PGP chat 

MUI based for rapid protoyping,  ideally Next.js with websockets.

- [ ] login via drag-and-dropping a key pair (or generating one)
- [ ] current user info is stored in session storage
- [ ] messages are only stored in local storage
- [ ] server has no idea what messages say, it just passes along encrypted payloads
